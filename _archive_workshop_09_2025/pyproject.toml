[project]
name = "meetup-workshop-mcp"
version = "0.1.0"
description = "Workshop on MCP at ScaDS.AI Living Lab (Meetup)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.12.0",
]

[tool.uv.workspace]
members = [
    "dashboard_client",
]
